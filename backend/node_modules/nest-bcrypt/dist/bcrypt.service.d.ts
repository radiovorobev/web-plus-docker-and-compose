/// <reference types="node" />
import { BcryptModuleOptions } from './types';
export declare class BcryptService {
    private readonly options;
    private salt;
    constructor(options: BcryptModuleOptions);
    compare(data: string | Buffer, hash: string): Promise<boolean>;
    compareSync(data: string | Buffer, hash: string): boolean;
    hash(data: string, salt?: string | number): Promise<string>;
    hashSync(data: string, salt?: string | number): string;
    getRounds(hash: string): number;
    genSalt(rounds: number, minor?: 'a' | 'b'): Promise<string>;
    genSaltSync(rounds: number, minor?: 'a' | 'b'): string;
}
